git init

git add .
git status
git commit -m "msg"
git push origin nome branch
git status

git checkout -b "nova branch " 
git checkout "nome branch existente"( na pasta local onde está a branch usada)
git branch -D "nomebranchusada"(deletará a branch usada do repo local)
git pull (tras do repo remoto pro local)

git commit -a (coloca absolutamente todos os arquivos pra serem enviados via push)

git pull origin main (atualiza a main usando o repo remoto)


